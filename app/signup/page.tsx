"use client";
import { useState } from "react";
import { createBrowserClient } from "@/lib/supabase/browser";
export default function Signup(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");const submit=async()=>{try{const {error}=await createBrowserClient().auth.signUp({email,password,options:{emailRedirectTo:`${location.origin}/account`}});if(error)throw error;setMessage("Account created. Check your email to confirm it if confirmation is enabled.");}catch{setMessage("We could not create your account. Use a valid email and a stronger password.")}};return <main><nav><a className="logo" href="/">Maithri <b>AI Prediction</b></a></nav><section className="reading"><div className="panel" style={{maxWidth:480}}><p className="eyebrow">SAVE YOUR READINGS</p><h1>Create an account</h1><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{message&&<p className="privacy-copy">{message}</p>}<button className="primary wide" onClick={submit}>Create account</button><p className="privacy-copy">Already registered? <a href="/login">Sign in</a>.</p></div></section></main>}
