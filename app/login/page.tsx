"use client";
import { useState } from "react";
import { createBrowserClient } from "@/lib/supabase/browser";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");const submit=async()=>{try{const {error}=await createBrowserClient().auth.signInWithPassword({email,password});if(error)throw error;location.assign("/account");}catch{setMessage("We could not sign you in. Check your email and password.")}};return <main><nav><a className="logo" href="/">Maithri <b>AI Prediction</b></a></nav><section className="reading"><div className="panel" style={{maxWidth:480}}><p className="eyebrow">WELCOME BACK</p><h1>Sign in</h1><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{message&&<p className="error">{message}</p>}<button className="primary wide" onClick={submit}>Sign in</button><p className="privacy-copy">New here? <a href="/signup">Create an account</a>.</p></div></section></main>}
