import { z } from "zod";
export const personSchema = z.object({ name: z.string().trim().min(1).max(100), gender: z.enum(["male", "female", "other"]), dob: z.string().date(), tob: z.string().max(10).optional().or(z.literal("")), birthPlace: z.string().trim().min(2).max(150), city: z.string().trim().max(100).optional(), education: z.string().trim().max(120).optional(), career: z.string().trim().max(120).optional(), relationshipGoal: z.string().trim().min(1).max(100), careerGoal: z.string().trim().max(100).optional(), lifestyle: z.array(z.string().max(60)).max(10), values: z.array(z.string().max(60)).max(10) });
export const analysisSchema = z.object({ personA: personSchema, personB: personSchema, modules: z.array(z.enum(["compatibility", "jataka", "numerology", "palm", "career", "relationship"])).min(1), consent: z.literal(true) });
